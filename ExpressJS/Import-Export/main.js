// const utils = require("./utils");
const { greet, add } = require("./utils");

// console.log(utils);

// console.log(utils.msg);

// console.log(utils.desc,utils.proce);

// utils(); //since we passed the function as an object we can now access the function directly

// console.log(utils.greet(), utils.add(2, 3));

greet();
add(2, 3);
