//1. create a function to find the number of characters in a string
// The function should take a string as an argument.
// The function should return the number of characters in the string.

function wordCount(str) {
  return str.length;
}

console.log(`The String has ${wordCount("Hello")} characters`);
